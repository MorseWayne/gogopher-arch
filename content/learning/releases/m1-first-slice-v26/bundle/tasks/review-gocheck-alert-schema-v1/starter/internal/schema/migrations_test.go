package schema

import "testing"

func TestMigrationContract(t *testing.T) {
	tests := []struct{ name string }{
		// TODO: add at least three named contract cases.
	}
	for _, test := range tests {
		t.Run(test.name, func(t *testing.T) {})
	}
}
