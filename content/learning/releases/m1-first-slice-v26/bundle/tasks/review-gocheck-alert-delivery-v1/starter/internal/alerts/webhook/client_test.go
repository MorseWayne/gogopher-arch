package webhook

import "testing"

func TestDeliverContract(t *testing.T) {
	tests := []struct{ name string }{
		// TODO: add at least five client boundary cases.
	}
	for _, test := range tests {
		t.Run(test.name, func(t *testing.T) {})
	}
}
