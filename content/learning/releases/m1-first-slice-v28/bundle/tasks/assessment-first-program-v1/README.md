# 独立完成第一个 Go 命令

实现 `greeting(name, attempts)`：

- 空名字按 `Gopher` 处理；
- 返回格式为 `welcome, <name>; attempts=<n>`；
- 必须使用传入参数，不能只满足公开样例。

提交前运行 Test、Build 和 Vet。在完成小结中至少使用 `Build`、`Test`、`Vet` 三个词，说明它们分别反馈什么，以及失败时你先检查什么。不要修改 `go.mod` 和测试文件。
