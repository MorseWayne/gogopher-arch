# 迁移到 Miniflux 后台刷新 Retry-After 策略

这次从 API 链切换到真实后台刷新链。基于 Miniflux v2.3.2 的 `internal/reader/fetcher` 与 `internal/reader/handler` 路径，让 `Retry-After` 同时支持正整数秒和 HTTP-date，并用注入时钟消除测试漂移；过去时间、非法值与非正上限返回零，合法延迟不能超过调用方上限。handler 只应在 429 响应上采用该值。
