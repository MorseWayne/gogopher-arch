# 变式复习：可取消的批量抓取

网络抓取比内存计算更容易长时间阻塞，因此 context 必须传入 `fetch`，不能只在派发任务前检查一次。父 deadline 或任一失败发生后，要让正在执行的抓取收到取消，并等待它们退出。

先独立重画生命周期，再实现。复习的目标不是记住某段 worker-pool 模板，而是能在新领域中重新证明：不会继续派发、不会丢失首个错误、不会提前返回、不会残留 goroutine。
