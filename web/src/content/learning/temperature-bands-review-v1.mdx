# 换成数值区间，复验同一组能力

上一次处理字符串与 Unicode，这次处理温度和闭区间。题材改变后仍要重新完成：命名类型、零值、循环、互斥分支和累计结果。

## 迁移检查

- `Celsius` 与 `Band` 是否保留了领域含义，而不是在函数内部退回一组无说明的裸值？
- `low >= high` 是否直接返回零值，且没有部分生成结果？
- 等于下界或上界时是否属于 normal？
- 每个输入是否恰好产生一条 `Reading`，每个非正常项是否恰好累计一次？

本次复习不提供提示、引用或参考解。请先独立写完，再用可见测试检查基本行为，最后提交异题验证。通过后会延长下一次维护间隔；失败则回到核心语义引导任务补强。
