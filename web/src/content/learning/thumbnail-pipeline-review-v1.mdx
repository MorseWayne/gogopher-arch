# 变式复习：从整数任务迁移到缩略图任务

接口名称变了，但结构不变量没有变：固定 worker 消费路径，结果 channel 只有一个明确的关闭时机，消费结束后没有后台工作残留。

先不要回看上次代码。用两分钟写出 channel ownership 表，再实现。提交后对照 [Go pipeline 指南](https://go.dev/blog/pipelines) 检查发送方、等待方和关闭方是否一致。
