# 用预算模型验证领域建模迁移

检查目标的状态由成功和失败驱动；预算的状态由支出和退款驱动。题材不同，但同样要求构造合法值、用方法表达操作、失败时保持原状态、从内部事实派生查询结果。

## 复验重点

- 非法名称和额度不能构造出可用 `Budget`。
- 非正金额、超额支出和超额退款要返回错误。
- 操作失败时 `spent` 不能被部分修改。
- `Remaining` 与 `Label` 始终反映同一份事实。

本次不提供提示、引用或参考解。请独立完成所有状态转换，并在提交前主动检查每个失败分支前后余额是否一致。
