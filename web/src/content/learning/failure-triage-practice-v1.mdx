# 从失败现场到最小修复

调试不是随机改代码直到测试变绿，而是逐步缩小假设空间：先确认失败类型，再找到最靠近业务代码的栈帧，最后把该行与输入边界对应起来。

运行公开测试，记录 panic 类型、顶部业务栈帧和触发输入。修复后再用空切片、单元素和多元素在脑中走一遍循环不变量。不要用 recover 掩盖根因。

完成后浏览官方 [Diagnostics](https://go.dev/doc/diagnostics) 的 Debugging 概览，建立测试失败、日志、调试器、profile 各自回答什么问题的地图；当前练习只训练失败测试与栈帧定位这一条最短链路。
