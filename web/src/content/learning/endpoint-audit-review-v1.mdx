# 延迟迁移：重新交付 endpointaudit

这次不提供代码骨架或公开契约测试。输入模型、输出协议、flag 名称和 package 名称都已变化；请从需求重新建立结构，不要机械替换 gocheck 的标识符。

先闭卷写出四个边界：spec 何时拒绝输入，probe 如何接收 context，output 如何保证确定性，app 如何映射 exit code。随后自行创建 module、实现和表格测试。提交阶段仍会运行隐藏集成测试与 Race Detector。

复验通过后，系统记录的是不同项目上下文中的迁移与保持证据。若卡住，先定位缺失的是项目拆解还是某个标准库 API，再决定是否回看资料。
