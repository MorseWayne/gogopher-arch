# 变式复习：在审计服务中重建抽象边界

这次不提供公开测试反馈。先写下两个契约：`Service` 对 sink 的最小需求是什么；`GroupBy` 哪部分逻辑与 `Event` 无关。然后再实现并提交。

隐藏检查会使用未在题面出现的元素类型和整数 key，也会让 sink 在第二次写入时失败。你需要同时保留顺序、错误传播、重复 key 分组和空输入可写 map 等行为。

若卡住，可回看本地[接口章节](../go-basics/ch07-interfaces.mdx)和官方[泛型教程](https://go.dev/doc/tutorial/generics)，但先尝试从已做过的通知交付题中回忆“使用方最小接口 + 算法所需最小约束”的判断方法。
