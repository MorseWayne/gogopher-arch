# 把第一段程序写成可测试的小函数

这次不再只补一个返回值。你要用 `bool` 参数和 `if` 分支决定状态，再把两个函数组合成稳定文本。

先让 `readiness(true)` 返回 `ready`，`readiness(false)` 返回 `retry`；再让 `report("build", true)` 返回 `build: ready`。

保持任务很小，是为了让你把注意力放在手写上：声明函数、使用参数、写分支、返回结果、根据测试修正。它们会在后续能力中继续扩展为数据处理和完整程序。
