# Copy this file to .env and adjust values when local defaults conflict with your machine.

# Published host ports
WEB_PORT=3000
GATEWAY_PORT=8080
SANDBOX_PORT=8081
POSTGRES_PORT=5432
REDIS_PORT=6379

# Internal service URLs used by Docker Compose
SANDBOX_URL=http://sandbox-engine:8081/execute
DB_URL=postgres://user:pass@postgres:5432/gogopher?sslmode=disable
REDIS_URL=redis:6379

# Local development defaults for bundled infrastructure
POSTGRES_USER=user
POSTGRES_PASSWORD=pass
POSTGRES_DB=gogopher
